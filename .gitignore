.env
__pycache__/
*.py[cod]
.venv/
.pytest_cache/
.coverage
htmlcov/
.ipynb_checkpoints/
logs/

# Raw and intermediate data stay local. The provenance ledgers are the exception.
data/bronze/*
!data/bronze/_manifest.jsonl
!data/bronze/_restatements.jsonl
data/silver/
# Compact, validated gold Parquets are versioned for the public dashboard.
data/quality/
data/analytics/
data/*.duckdb
data/*.duckdb.wal

# Serialized estimators stay local; metadata and evaluation receipts are versioned.
models/*/model.pkl

# Local dashboard/runtime state.
.streamlit/secrets.toml
