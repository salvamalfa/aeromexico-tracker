// AUTO-GENERATED by web/scripts/gen-types.mjs from contracts/web/*.schema.json.
// Do not edit by hand; run `npm run gen:types` after changing a schema.

/**
 * Describes site/publication_manifest.json, written by src.publish.gate. Lists the code commit, each contracts/web/ file's own SHA-256 (its 'version'), every other file under site/ with its SHA-256 and byte size (sorted by path, the manifest file itself excluded), and the analysis-manifest entries the currently published page also carries (period_id, version, content_hash, evidence_fingerprint, approval_event, audit_hash) — see docs/arquitectura/auditoria-arquitectura-20260926.md §4.2 punto 5 and src/publish/README.md.
 */
export interface ManifiestoDePublicacionDeSiteV1 {
  schema_version: "publication_manifest_v1";
  code_commit: string;
  generated_at: string;
  contracts: {
    [k: string]: string;
  };
  files: FileEntry[];
  analysis_manifest: AnalysisEntry[];
}
export interface FileEntry {
  path: string;
  sha256: string;
  bytes: number;
}
export interface AnalysisEntry {
  period_id: string;
  version: string;
  content_hash: string;
  evidence_fingerprint: string;
  approval_event: string;
  audit_hash: string;
}
