version: stage9_v1.0.0
tables:
  dim_carrier:
    grain: [carrier_key]
    primary_key: [carrier_key]
    columns:
      carrier_key: {type: string, nullable: false, description: Clave estable de la entidad aérea.}
      carrier_name: {type: string, nullable: false, description: Razón o nombre operacional.}
      carrier_name_short: {type: string, nullable: false, description: Nombre corto para visualización.}
      iata_code: {type: string, nullable: true, description: Código IATA de dos caracteres.}
      icao_code: {type: string, nullable: true, description: Código ICAO.}
      country: {type: string, nullable: true, description: País base de la entidad.}
      business_model: {type: string, nullable: false, description: Modelo de negocio normalizado.}
      is_public: {type: bool, nullable: false, description: Indica si cotiza en bolsa.}
      ticker: {type: string, nullable: true, description: Ticker principal usado por el proyecto.}
      exchange: {type: string, nullable: true, description: Bolsa principal o combinación de bolsas.}
      cik: {type: string, nullable: true, description: CIK de SEC, si existe.}
      reporting_standard: {type: string, nullable: true, description: IFRS o US-GAAP.}
      reporting_currency: {type: string, nullable: true, description: Moneda funcional de reporte.}
      unit_system: {type: string, nullable: false, description: Sistema de unidades preferido por la fuente.}
      fiscal_year_end_month: {type: int, nullable: true, description: Mes de cierre del ejercicio fiscal.}
      parent_carrier_key: {type: string, nullable: true, description: Clave del grupo consolidante.}
      is_peer: {type: bool, nullable: false, description: Identifica peers del análisis.}
      is_focus: {type: bool, nullable: false, description: Identifica la compañía objetivo.}
      valid_from: {type: date, nullable: false, description: Inicio de vigencia de la identidad.}
      valid_to: {type: date, nullable: true, description: Fin de vigencia de la identidad.}
      is_current: {type: bool, nullable: false, description: Versión vigente de la identidad.}
  dim_period:
    grain: [period_id]
    primary_key: [period_id]
    columns:
      period_id: {type: string, nullable: false, description: Clave de mes, trimestre, año o TTM.}
      period_type: {type: string, nullable: false, allowed_values: [month, quarter, year, ttm], description: month, quarter, year o ttm.}
      period_start_date: {type: date, nullable: false, description: Primer día incluido.}
      period_end_date: {type: date, nullable: false, description: Último día incluido.}
      year: {type: int, nullable: false, description: Año calendario de cierre.}
      quarter: {type: int, nullable: true, description: Trimestre calendario de cierre.}
      month: {type: int, nullable: true, description: Mes calendario de cierre.}
      days_in_period: {type: int, nullable: false, description: Días calendario incluidos.}
      is_covid_period: {type: bool, nullable: false, description: Periodo entre marzo de 2020 y diciembre de 2021.}
      prior_period_id: {type: string, nullable: true, description: Periodo comparable inmediatamente anterior.}
      prior_year_period_id: {type: string, nullable: true, description: Mismo periodo del año previo.}
      fiscal_period_id: {type: string, nullable: false, description: Identificador fiscal por defecto.}
      calendar_period_id: {type: string, nullable: false, description: Identificador calendario.}
      easter_date: {type: date, nullable: false, description: Domingo de Pascua del año.}
      easter_quarter: {type: int, nullable: false, description: Trimestre que contiene Pascua.}
      easter_days_in_q1: {type: int, nullable: false, description: Días de la ventana Domingo de Ramos a lunes de Pascua que caen en Q1.}
      easter_days_in_q2: {type: int, nullable: false, description: Días de la ventana Domingo de Ramos a lunes de Pascua que caen en Q2.}
  dim_metric:
    grain: [metric_key]
    primary_key: [metric_key]
    columns:
      metric_key: {type: string, nullable: false, description: Clave estable de la métrica.}
      metric_name_es: {type: string, nullable: false, description: Nombre en español.}
      metric_name_en: {type: string, nullable: false, description: Nombre en inglés.}
      metric_category: {type: string, nullable: false, description: Categoría analítica.}
      unit_normalized: {type: string, nullable: false, description: Unidad normalizada esperada.}
      consolidation_method: {type: string, nullable: false, allowed_values: [sum, weighted, latest, non_additive], description: Regla explícita para consolidar la métrica entre entidades.}
      formula: {type: string, nullable: true, description: Fórmula literal.}
      higher_is_better: {type: bool, nullable: true, description: Sentido favorable; nulo cuando depende del contexto.}
      business_interpretation_up: {type: string, nullable: false, description: Lectura de negocio cuando sube.}
      business_interpretation_down: {type: string, nullable: false, description: Lectura de negocio cuando baja.}
      why_it_matters: {type: string, nullable: false, description: Relevancia de negocio.}
      typical_range_network: {type: string, nullable: true, description: Referencia aproximada para network carriers.}
      typical_range_ulcc: {type: string, nullable: true, description: Referencia aproximada para ULCC.}
      caveats: {type: string, nullable: false, description: Advertencias de comparabilidad y definición.}
      display_format: {type: string, nullable: false, description: Formato de presentación.}
      display_order: {type: int, nullable: false, description: Orden sugerido en UI.}
      glossary_section: {type: string, nullable: true, description: Encabezado fuente en el glosario.}
      is_dashboard_metric: {type: bool, nullable: false, description: Métrica prevista para mostrarse en el dashboard.}
  dim_route:
    grain: [route_key]
    primary_key: [route_key]
    columns:
      route_key: {type: string, nullable: false, description: Ruta direccional origen-destino.}
      origin_iata: {type: string, nullable: false, description: Aeropuerto de origen.}
      dest_iata: {type: string, nullable: false, description: Aeropuerto de destino.}
      origin_country: {type: string, nullable: false, description: País de origen.}
      dest_country: {type: string, nullable: false, description: País de destino.}
      distance_km: {type: float, nullable: false, description: Distancia mediana en kilómetros.}
      distance_miles: {type: float, nullable: false, description: Distancia mediana en millas estatuta.}
      is_domestic_mx: {type: bool, nullable: false, description: Ambos extremos están en México.}
      is_transborder_us: {type: bool, nullable: false, description: Ruta México-Estados Unidos.}
      is_international: {type: bool, nullable: false, description: Cruza una frontera.}
      market_key: {type: string, nullable: false, description: Mercado bidireccional canónico.}
  dim_airport:
    grain: [airport_iata]
    primary_key: [airport_iata]
    columns:
      airport_iata: {type: string, nullable: false, description: Código IATA de un aeropuerto real.}
      airport_icao: {type: string, nullable: true, description: Código ICAO.}
      name: {type: string, nullable: true, description: Nombre del aeropuerto.}
      city: {type: string, nullable: true, description: Ciudad.}
      country: {type: string, nullable: true, description: País.}
      latitude: {type: float, nullable: true, description: Latitud.}
      longitude: {type: float, nullable: true, description: Longitud.}
      elevation: {type: float, nullable: true, description: Elevación publicada.}
      type: {type: string, nullable: true, description: Tipo de instalación.}
      operator_group: {type: string, nullable: true, description: Grupo operador mexicano.}
      source_system: {type: string, nullable: false, description: Sistema fuente.}
      source_file: {type: string, nullable: false, description: Archivo bronze fuente.}
      source_hash: {type: string, nullable: false, description: SHA-256 fuente.}
      ingested_at: {type: datetime, nullable: false, description: Fecha de ingesta.}
      parser_version: {type: string, nullable: false, description: Versión del parser.}
  dim_airport_group:
    stage: 6
    grain: [airport_group_key]
    primary_key: [airport_group_key]
    columns:
      airport_group_key: {type: string, nullable: false, description: Clave estable del grupo aeroportuario.}
      airport_group_name: {type: string, nullable: false, description: Nombre de negocio del grupo aeroportuario.}
      country: {type: string, nullable: false, description: País de los aeropuertos incluidos.}
      source_system: {type: string, nullable: false, description: Sistemas fuente contribuyentes.}
      source_file: {type: string, nullable: false, description: Archivos fuente contribuyentes.}
      source_hash: {type: string, nullable: false, regex: '^[0-9a-f]{64}$', description: Fingerprint determinista del linaje agregado.}
      ingested_at: {type: datetime, nullable: false, description: Última fecha de ingesta contribuyente.}
  dim_events:
    grain: [event_date, title]
    primary_key: [event_date, title]
    columns:
      event_date: {type: datetime, nullable: false, description: Fecha del evento.}
      event_type: {type: string, nullable: false, description: Tipo de evento.}
      event_category: {type: string, nullable: false, description: Categoría analítica.}
      title: {type: string, nullable: false, description: Título corto.}
      description: {type: string, nullable: false, description: Descripción.}
      affected_carriers: {type: string, nullable: false, description: Entidades afectadas.}
      impact_direction: {type: string, nullable: false, description: Dirección esperada del impacto.}
      source_url: {type: string, nullable: false, description: Fuente primaria.}
      confidence: {type: string, nullable: false, description: Confianza cualitativa.}
      source_system: {type: string, nullable: false, description: Sistema fuente.}
      source_file: {type: string, nullable: true, description: Archivo bronze fuente.}
      source_hash: {type: string, nullable: true, description: SHA-256 fuente.}
      ingested_at: {type: datetime, nullable: false, description: Fecha de ingesta.}
      parser_version: {type: string, nullable: false, description: Versión del parser.}
  dim_fx_period:
    grain: [period_id, currency_pair]
    primary_key: [period_id, currency_pair]
    columns:
      rate_avg: {type: float, nullable: false, description: Tipo promedio del periodo.}
      rate_close: {type: float, nullable: false, description: Último tipo disponible del periodo.}
      rate_min: {type: float, nullable: false, description: Mínimo del periodo.}
      rate_max: {type: float, nullable: false, description: Máximo del periodo.}
      period_id: {type: string, nullable: false, description: Periodo calendario.}
      period_type: {type: string, nullable: false, description: month, quarter o year.}
      currency_pair: {type: string, nullable: false, description: Par cotizado como moneda local por USD.}
      pnl_conversion_method: {type: string, nullable: false, description: Método para flujos de P&L.}
      balance_conversion_method: {type: string, nullable: false, description: Método para saldos.}
  dim_fuel_period:
    grain: [period_id]
    primary_key: [period_id]
    columns:
      price_avg_usd_per_gallon: {type: float, nullable: false, description: Precio promedio.}
      price_close_usd_per_gallon: {type: float, nullable: false, description: Precio de cierre.}
      price_min_usd_per_gallon: {type: float, nullable: false, description: Precio mínimo.}
      price_max_usd_per_gallon: {type: float, nullable: false, description: Precio máximo.}
      period_id: {type: string, nullable: false, description: Periodo calendario.}
      period_type: {type: string, nullable: false, description: month o quarter.}
      price_avg_yoy_pct: {type: float, nullable: true, description: Variación interanual del promedio.}
  fx_business_calendar:
    grain: [date, currency_pair]
    primary_key: [date, currency_pair]
    columns:
      date: {type: datetime, nullable: false, description: Día hábil.}
      rate_close: {type: float, nullable: false, description: Tipo publicado o arrastrado.}
      is_published: {type: bool, nullable: false, description: El valor fue publicado ese día.}
      fill_method: {type: string, nullable: false, description: Método de llenado.}
      currency_pair: {type: string, nullable: false, description: Par de monedas.}
  fuel_business_calendar:
    grain: [date]
    primary_key: [date]
    columns:
      date: {type: datetime, nullable: false, description: Día hábil.}
      price_usd_per_gallon: {type: float, nullable: false, description: Precio publicado o arrastrado.}
      is_published: {type: bool, nullable: false, description: El valor fue publicado ese día.}
      fill_method: {type: string, nullable: false, description: Método de llenado.}
  dim_source:
    stage: 6
    grain: [source_key]
    primary_key: [source_key]
    columns:
      source_key: {type: string, nullable: false, regex: '^[a-z][a-z0-9_]*$', description: Clave canónica de la fuente pública o curada.}
      source_systems: {type: string, nullable: false, description: Lista JSON de identificadores técnicos asociados.}
      display_name: {type: string, nullable: false, description: Nombre comprensible de la fuente.}
      institution: {type: string, nullable: false, description: Institución responsable.}
      business_description: {type: string, nullable: false, description: Información de negocio aportada.}
      coverage: {type: string, nullable: false, description: Cobertura temporal y geográfica.}
      update_frequency: {type: string, nullable: false, description: Frecuencia esperada de actualización.}
      access_method: {type: string, nullable: false, description: Método autorizado de obtención.}
      official_page_url: {type: string, nullable: true, description: Página HTTPS oficial cuando existe.}
      artifact_link_policy: {type: string, nullable: false, allowed_values: [direct, landing_page_only, not_applicable], description: Política para exponer enlaces de artefactos.}
      limitations: {type: string, nullable: false, description: Limitaciones materiales de cobertura o acceso.}
      source_kind: {type: string, nullable: false, allowed_values: [public, curated, derived, error_evidence], description: Naturaleza de la fuente.}
      artifact_expected: {type: bool, nullable: false, description: Indica si la fuente debe producir archivos Bronze.}
      is_active: {type: bool, nullable: false, description: Indica si la fuente sigue activa.}
  dim_source_priority:
    stage: 6
    grain: [data_domain, source_system]
    primary_key: [data_domain, source_system]
    columns:
      data_domain: {type: string, nullable: false, description: Dominio de datos donde aplica la precedencia.}
      source_system: {type: string, nullable: false, description: Fuente o comodín por defecto.}
      priority: {type: int, nullable: false, min: 0, description: Prioridad ascendente; menor es preferible.}
      is_default: {type: bool, nullable: false, description: Identifica la regla de respaldo del dominio.}
      source_priority_order: {type: string, nullable: false, allowed_values: [asc, desc], description: Orden de la prioridad de fuente.}
      is_preliminary_order: {type: string, nullable: false, allowed_values: [asc, desc], description: Orden de preferencia para datos preliminares.}
      confidence_order: {type: string, nullable: false, allowed_values: [asc, desc], description: Orden de preferencia para confianza.}
      ingested_at_order: {type: string, nullable: false, allowed_values: [asc, desc], description: Orden de desempate por ingesta.}
      rationale: {type: string, nullable: false, description: Justificación de la precedencia.}
  fact_carrier_metrics:
    grain: [carrier_key, period_id, metric_key, segment, source_system, valid_from]
    primary_key: [record_id]
    foreign_keys:
      - columns: [carrier_key]
        references: {table: dim_carrier, columns: [carrier_key]}
      - columns: [period_id]
        references: {table: dim_period, columns: [period_id]}
      - columns: [metric_key]
        references: {table: dim_metric, columns: [metric_key]}
    invariants:
      - name: homogeneous_scd2_history
        kind: scd2_history
        key_columns: [carrier_key, period_id, metric_key, segment, source_system]
        value_columns: [value]
    columns:
      record_id: {type: string, nullable: false, unique: true, regex: '^rec_[0-9a-f]{64}$', description: Identificador estable calculado con la tabla y su grano natural.}
      carrier_key: {type: string, nullable: false, description: Aerolínea o grupo.}
      period_id: {type: string, nullable: false, description: Periodo reportado.}
      calendar_period_id: {type: string, nullable: false, description: Periodo calendario comparable.}
      fiscal_period_id: {type: string, nullable: false, description: Periodo fiscal de la fuente.}
      period_type: {type: string, nullable: false, allowed_values: [month, quarter, year, ttm], description: Granularidad temporal.}
      period_start_date: {type: date, nullable: false, description: Inicio del periodo.}
      period_end_date: {type: date, nullable: false, description: Fin del periodo.}
      metric_key: {type: string, nullable: false, description: Métrica.}
      segment: {type: string, nullable: false, allowed_values: [total, domestic, international, transborder], description: total, domestic, international o transborder.}
      value: {type: float, nullable: true, description: Valor normalizado en la unidad declarada.}
      value_metric: {type: float, nullable: true, description: Equivalente métrico cuando aplica.}
      value_imperial: {type: float, nullable: true, description: Equivalente imperial cuando aplica.}
      value_as_reported: {type: float, nullable: true, description: Valor numérico publicado antes de escala.}
      unit_as_reported: {type: string, nullable: true, description: Unidad literal publicada.}
      unit_normalized: {type: string, nullable: false, description: Unidad normalizada de value.}
      currency: {type: string, nullable: true, description: Moneda original.}
      value_original_currency: {type: float, nullable: true, description: Valor ya escalado en moneda original.}
      value_usd: {type: float, nullable: true, description: Equivalente en USD.}
      fx_rate_used: {type: float, nullable: true, description: Tipo de cambio aplicado.}
      fx_rate_type: {type: string, nullable: true, allowed_values: [average, close], description: average o close.}
      is_derived: {type: bool, nullable: false, description: Valor calculado por el pipeline.}
      is_preliminary: {type: bool, nullable: false, description: Fuente lo marca preliminar.}
      is_estimated: {type: bool, nullable: false, description: Valor estimado y etiquetado.}
      derivation_formula: {type: string, nullable: true, description: Fórmula para valores derivados.}
      valid_from: {type: datetime, nullable: false, description: Inicio de vigencia SCD2.}
      valid_to: {type: datetime, nullable: true, description: Fin de vigencia SCD2.}
      is_current: {type: bool, nullable: false, description: Versión vigente.}
      restatement_count: {type: int, nullable: false, min: 0, description: Número de reexpresiones previas.}
      source_system: {type: string, nullable: false, description: Sistema fuente.}
      source_file: {type: string, nullable: false, description: Archivo fuente o agregado silver.}
      source_hash: {type: string, nullable: false, description: SHA-256 de linaje.}
      ingested_at: {type: datetime, nullable: false, description: Fecha de ingesta.}
      confidence: {type: float, nullable: false, min: 0, max: 1, description: Confianza de 0 a 1.}
  fact_route_traffic:
    grain: [carrier_key, route_key, period_id, aircraft_type, service_class]
    primary_key: [record_id]
    foreign_keys:
      - columns: [carrier_key]
        references: {table: dim_carrier, columns: [carrier_key]}
      - columns: [route_key]
        references: {table: dim_route, columns: [route_key]}
      - columns: [period_id]
        references: {table: dim_period, columns: [period_id]}
    columns:
      record_id: {type: string, nullable: false, unique: true, regex: '^rec_[0-9a-f]{64}$', description: Identificador estable calculado con la tabla y su grano natural.}
      carrier_key: {type: string, nullable: false, description: Aerolínea.}
      route_key: {type: string, nullable: false, description: Ruta direccional.}
      period_id: {type: string, nullable: false, description: Mes calendario.}
      calendar_period_id: {type: string, nullable: false, description: Mes calendario comparable.}
      fiscal_period_id: {type: string, nullable: false, description: Mes fiscal de la fuente; igual al calendario para T-100.}
      aircraft_type: {type: int, nullable: false, description: Código BTS de aeronave.}
      service_class: {type: string, nullable: false, description: Clase de servicio BTS.}
      departures_scheduled: {type: float, nullable: false, description: Salidas programadas.}
      departures_performed: {type: float, nullable: false, description: Salidas realizadas.}
      seats: {type: float, nullable: false, description: Asientos ofrecidos.}
      passengers: {type: float, nullable: false, description: Pasajeros transportados.}
      freight_kg: {type: float, nullable: false, description: Carga convertida de libras a kg.}
      mail_kg: {type: float, nullable: false, description: Correo convertido de libras a kg.}
      asm_miles: {type: float, nullable: false, description: Available seat miles.}
      ask_km: {type: float, nullable: false, description: Available seat kilometers.}
      rpm_miles: {type: float, nullable: false, description: Revenue passenger miles.}
      rpk_km: {type: float, nullable: false, description: Revenue passenger kilometers.}
      load_factor: {type: float, nullable: true, description: RPM dividido entre ASM.}
      distance_miles: {type: float, nullable: false, description: Distancia mediana de los segmentos.}
      distance_km: {type: float, nullable: false, description: Distancia en kilómetros.}
      source_system: {type: string, nullable: false, description: Sistema fuente.}
      source_file: {type: string, nullable: false, description: Archivo silver agregado.}
      source_hash: {type: string, nullable: false, description: Hash determinista del linaje agregado.}
      ingested_at: {type: datetime, nullable: false, description: Última ingesta contribuyente.}
  fact_airport_traffic:
    grain: [airport_iata, period_id, operator_group, source_system]
    primary_key: [record_id]
    foreign_keys:
      - columns: [airport_iata]
        references: {table: dim_airport, columns: [airport_iata]}
      - columns: [period_id]
        references: {table: dim_period, columns: [period_id]}
    columns:
      record_id: {type: string, nullable: false, unique: true, regex: '^rec_[0-9a-f]{64}$', description: Identificador estable calculado con la tabla y su grano natural.}
      airport_iata: {type: string, nullable: false, description: Código IATA de un aeropuerto real.}
      period_id: {type: string, nullable: false, description: Mes calendario.}
      calendar_period_id: {type: string, nullable: false, description: Mes calendario comparable.}
      fiscal_period_id: {type: string, nullable: false, description: Mes fiscal de la fuente; igual al calendario.}
      passengers_domestic: {type: int, nullable: true, description: Pasajeros domésticos.}
      passengers_international: {type: int, nullable: true, description: Pasajeros internacionales.}
      passengers_total: {type: int, nullable: true, description: Pasajeros totales.}
      cargo_tons: {type: float, nullable: true, description: Carga en toneladas.}
      operations: {type: int, nullable: true, description: Operaciones aéreas.}
      operator_group: {type: string, nullable: false, description: Operador o grupo.}
      country: {type: string, nullable: false, description: País.}
      source_system: {type: string, nullable: false, description: Sistema fuente.}
      source_file: {type: string, nullable: false, description: Archivo fuente.}
      source_hash: {type: string, nullable: false, description: SHA-256 fuente.}
      ingested_at: {type: datetime, nullable: false, description: Fecha de ingesta.}
  fact_airport_group_traffic:
    stage: 6
    grain: [airport_group_key, period_id, source_system]
    primary_key: [record_id]
    foreign_keys:
      - columns: [airport_group_key]
        references: {table: dim_airport_group, columns: [airport_group_key]}
      - columns: [period_id]
        references: {table: dim_period, columns: [period_id]}
    columns:
      record_id: {type: string, nullable: false, unique: true, regex: '^rec_[0-9a-f]{64}$', description: Identificador estable calculado con la tabla y su grano natural.}
      airport_group_key: {type: string, nullable: false, description: Grupo aeroportuario observado.}
      period_id: {type: string, nullable: false, description: Mes calendario.}
      calendar_period_id: {type: string, nullable: false, description: Mes calendario comparable.}
      fiscal_period_id: {type: string, nullable: false, description: Mes fiscal de referencia; igual al calendario.}
      passengers_domestic: {type: int, nullable: true, description: Pasajeros domésticos del grupo.}
      passengers_international: {type: int, nullable: true, description: Pasajeros internacionales del grupo.}
      passengers_total: {type: int, nullable: true, description: Pasajeros totales del grupo.}
      cargo_tons: {type: float, nullable: true, description: Carga del grupo en toneladas.}
      operations: {type: int, nullable: true, description: Operaciones aéreas del grupo.}
      country: {type: string, nullable: false, description: País.}
      source_system: {type: string, nullable: false, description: Sistema fuente.}
      source_file: {type: string, nullable: false, description: Archivo fuente.}
      source_hash: {type: string, nullable: false, description: SHA-256 fuente.}
      ingested_at: {type: datetime, nullable: false, description: Fecha de ingesta.}
  fact_market_data:
    grain: [carrier_key, date]
    primary_key: [record_id]
    foreign_keys:
      - columns: [carrier_key]
        references: {table: dim_carrier, columns: [carrier_key]}
    columns:
      record_id: {type: string, nullable: false, unique: true, regex: '^rec_[0-9a-f]{64}$', description: Identificador estable calculado con la tabla y su grano natural.}
      carrier_key: {type: string, nullable: false, description: Emisor.}
      ticker: {type: string, nullable: false, description: Ticker observado.}
      date: {type: datetime, nullable: false, description: Sesión de mercado.}
      calendar_period_id: {type: string, nullable: false, description: Fecha calendario ISO de la sesión.}
      fiscal_period_id: {type: string, nullable: false, description: Fecha fiscal de referencia; igual a la fecha calendario.}
      close: {type: float, nullable: false, description: Precio de cierre.}
      adj_close: {type: float, nullable: false, description: Cierre ajustado.}
      volume: {type: int, nullable: false, description: Volumen.}
      currency: {type: string, nullable: false, description: Moneda de cotización.}
      return_1d: {type: float, nullable: true, description: Rendimiento diario.}
      return_ytd: {type: float, nullable: true, description: Rendimiento desde inicio de año.}
      volatility_30d: {type: float, nullable: true, description: Volatilidad anualizada de 30 sesiones.}
      source_system: {type: string, nullable: false, description: Sistema fuente.}
      source_file: {type: string, nullable: false, description: Archivo fuente.}
      source_hash: {type: string, nullable: false, description: SHA-256 fuente.}
      ingested_at: {type: datetime, nullable: false, description: Fecha de ingesta.}
  fact_macro:
    grain: [period_id, indicator_key, aggregation]
    primary_key: [record_id]
    foreign_keys:
      - columns: [period_id]
        references: {table: dim_period, columns: [period_id]}
    columns:
      record_id: {type: string, nullable: false, unique: true, regex: '^rec_[0-9a-f]{64}$', description: Identificador estable calculado con la tabla y su grano natural.}
      period_id: {type: string, nullable: false, description: Mes o trimestre.}
      period_type: {type: string, nullable: false, allowed_values: [month, quarter], description: month o quarter.}
      calendar_period_id: {type: string, nullable: false, description: Periodo calendario comparable.}
      fiscal_period_id: {type: string, nullable: false, description: Periodo fiscal de referencia; igual al calendario.}
      indicator_key: {type: string, nullable: false, description: Serie exógena.}
      value: {type: float, nullable: false, description: Valor agregado.}
      unit: {type: string, nullable: false, description: Unidad.}
      aggregation: {type: string, nullable: false, allowed_values: [average, close], description: average o close.}
      source_system: {type: string, nullable: false, description: Sistema fuente.}
      source_file: {type: string, nullable: false, description: Archivo silver fuente.}
      source_hash: {type: string, nullable: false, description: Hash determinista del linaje.}
      ingested_at: {type: datetime, nullable: false, description: Última ingesta contribuyente.}
  fact_data_quality_issues:
    grain: [issue_signature]
    primary_key: [record_id]
    columns:
      record_id: {type: string, nullable: false, unique: true, regex: '^rec_[0-9a-f]{64}$', description: Identificador estable calculado con la tabla y su grano natural.}
      issue_id: {type: string, nullable: false, description: Hash estable del issue.}
      issue_signature: {type: string, nullable: false, unique: true, regex: '^[0-9a-f]{64}$', description: Firma semántica estable usada para deduplicar incidencias.}
      issue_origin: {type: string, nullable: false, allowed_values: [operational_ledger, derived_reconciliation], description: Mecanismo que detectó la incidencia.}
      issue_type: {type: string, nullable: false, description: Tipo de problema.}
      severity: {type: string, nullable: false, allowed_values: [info, warning, error, critical], description: Severidad normalizada.}
      status: {type: string, nullable: false, allowed_values: [open, resolved, accepted], description: Estado canónico de la incidencia.}
      resolved: {type: bool, nullable: false, description: Indica si la incidencia fue resuelta.}
      layer: {type: string, nullable: false, description: Capa donde se detectó.}
      dataset_name: {type: string, nullable: false, description: Dataset afectado.}
      source_system: {type: string, nullable: false, description: Fuente afectada.}
      source_file: {type: string, nullable: true, description: Archivo involucrado.}
      carrier_key: {type: string, nullable: true, description: Aerolínea afectada.}
      period_id: {type: string, nullable: true, description: Periodo afectado.}
      calendar_period_id: {type: string, nullable: true, description: Periodo calendario afectado.}
      fiscal_period_id: {type: string, nullable: true, description: Periodo fiscal afectado.}
      metric_key: {type: string, nullable: true, description: Métrica afectada.}
      observed_value: {type: float, nullable: true, description: Valor observado.}
      expected_value: {type: float, nullable: true, description: Valor de comparación.}
      difference_pct: {type: float, nullable: true, description: Diferencia relativa.}
      affected_rows: {type: int, nullable: true, min: 0, description: Número de filas afectadas cuando puede determinarse.}
      description: {type: string, nullable: false, description: Explicación accionable.}
      evidence: {type: string, nullable: false, description: Evidencia estructurada reproducible.}
      detected_at: {type: datetime, nullable: false, description: Fecha reproducible de la evidencia.}
      resolved_at: {type: datetime, nullable: true, description: Fecha de resolución cuando existe.}
  fact_forecasts:
    stage: 7
    grain: [model_run_id, model_name, carrier_key, metric_key, period_id, is_backtest]
    primary_key: [record_id]
    foreign_keys:
      - columns: [carrier_key]
        references: {table: dim_carrier, columns: [carrier_key]}
      - columns: [metric_key]
        references: {table: dim_metric, columns: [metric_key]}
    columns:
      record_id: {type: string, nullable: false, unique: true, regex: '^rec_[0-9a-f]{64}$', description: Identificador estable calculado con la tabla y su grano natural.}
      model_run_id: {type: string, nullable: false, description: Identificador determinista de corrida.}
      model_name: {type: string, nullable: false, description: Modelo que superó el gate de publicación.}
      carrier_key: {type: string, nullable: false, description: Aerolínea objetivo.}
      metric_key: {type: string, nullable: false, description: Métrica pronosticada.}
      period_id: {type: string, nullable: false, description: Mes de backtest o pronóstico.}
      forecast_value: {type: float, nullable: false, description: Pronóstico puntual.}
      lower_80: {type: float, nullable: false, description: Límite inferior al 80 por ciento.}
      upper_80: {type: float, nullable: false, description: Límite superior al 80 por ciento.}
      lower_95: {type: float, nullable: false, description: Límite inferior al 95 por ciento.}
      upper_95: {type: float, nullable: false, description: Límite superior al 95 por ciento.}
      is_backtest: {type: bool, nullable: false, description: Distingue evaluación histórica de futuro.}
      actual_value: {type: float, nullable: true, description: Real observado, solo backtest.}
      error: {type: float, nullable: true, description: Real menos pronóstico.}
      abs_pct_error: {type: float, nullable: true, description: Error porcentual absoluto.}
      trained_through_period: {type: string, nullable: false, description: Último periodo visible al entrenar.}
      features_used: {type: string, nullable: false, description: Variables disponibles al origen.}
      trained_at: {type: datetime, nullable: false, description: Marca reproducible de la evidencia fuente.}
  dim_model_performance:
    stage: 7
    grain: [model_run_id, model_name, carrier_key, metric_key, evaluation_split]
    primary_key: [record_id]
    foreign_keys:
      - columns: [carrier_key]
        references: {table: dim_carrier, columns: [carrier_key]}
      - columns: [metric_key]
        references: {table: dim_metric, columns: [metric_key]}
    columns:
      record_id: {type: string, nullable: false, unique: true, regex: '^rec_[0-9a-f]{64}$', description: Identificador estable del resultado de evaluación.}
      model_run_id: {type: string, nullable: false, description: Identificador determinista de corrida.}
      model_name: {type: string, nullable: false, description: Modelo evaluado.}
      carrier_key: {type: string, nullable: false, description: Aerolínea objetivo.}
      metric_key: {type: string, nullable: false, description: Métrica evaluada.}
      evaluation_split: {type: string, nullable: false, allowed_values: [validation, test], description: Conjunto de evaluación; nunca train.}
      validation_smape: {type: float, nullable: false, description: sMAPE de validación usado para selección.}
      mape: {type: float, nullable: false, description: MAPE de test.}
      smape: {type: float, nullable: false, description: sMAPE de test.}
      mae: {type: float, nullable: false, description: MAE de test.}
      rmse: {type: float, nullable: false, description: RMSE de test.}
      mase: {type: float, nullable: false, description: MASE contra escala estacional.}
      observations: {type: int, nullable: false, description: Orígenes de test.}
      is_baseline: {type: bool, nullable: false, description: Identifica baseline.}
      beats_seasonal_naive: {type: bool, nullable: false, description: Gate de desempeño en test.}
      is_published: {type: bool, nullable: false, description: Modelo visible en dashboard.}
      trained_through_period: {type: string, nullable: false, description: Corte de entrenamiento antes del test.}
  fact_report_language:
    stage: 7
    grain: [carrier_key, period_id, accession_number, section]
    primary_key: [record_id]
    foreign_keys:
      - columns: [carrier_key]
        references: {table: dim_carrier, columns: [carrier_key]}
      - columns: [period_id]
        references: {table: dim_period, columns: [period_id]}
    columns:
      record_id: {type: string, nullable: false, unique: true, regex: '^rec_[0-9a-f]{64}$', description: Identificador estable calculado con la tabla y su grano natural.}
      carrier_key: {type: string, nullable: false, description: Emisor del reporte.}
      period_id: {type: string, nullable: false, description: Periodo del reporte.}
      accession_number: {type: string, nullable: false, description: Accession SEC.}
      section: {type: string, nullable: false, description: Sección analizada.}
      report_type: {type: string, nullable: false, description: Resultados o tráfico.}
      word_count: {type: int, nullable: false, description: Palabras tokenizadas.}
      readability_score: {type: float, nullable: false, description: Flesch Reading Ease aproximado.}
      passive_ratio: {type: float, nullable: false, description: Construcciones pasivas aproximadas por oración.}
      numeric_density: {type: float, nullable: false, description: Tokens numéricos sobre tokens totales.}
      lm_positive_ratio: {type: float, nullable: false, description: Proporción positiva Loughran-McDonald.}
      lm_negative_ratio: {type: float, nullable: false, description: Proporción negativa Loughran-McDonald.}
      lm_uncertainty_ratio: {type: float, nullable: false, description: Proporción de incertidumbre Loughran-McDonald.}
      lm_litigious_ratio: {type: float, nullable: false, description: Proporción litigiosa Loughran-McDonald.}
      lm_constraining_ratio: {type: float, nullable: false, description: Proporción de restricción Loughran-McDonald.}
      top_terms_json: {type: string, nullable: false, description: Términos TF-IDF principales.}
      new_terms_json: {type: string, nullable: false, description: Vocabulario nuevo frente a reporte comparable previo.}
      dropped_terms_json: {type: string, nullable: false, description: Vocabulario ausente frente a reporte comparable previo.}
      source_file: {type: string, nullable: false, description: Archivo SEC fuente.}
      source_hash: {type: string, nullable: false, description: Hash del archivo fuente.}
  fact_anomalies:
    stage: 7
    grain: [anomaly_id]
    primary_key: [record_id]
    columns:
      record_id: {type: string, nullable: false, unique: true, regex: '^rec_[0-9a-f]{64}$', description: Identificador estable calculado con la tabla y su grano natural.}
      anomaly_id: {type: string, nullable: false, description: Hash estable de anomalía.}
      anomaly_type: {type: string, nullable: false, description: Familia de detección.}
      entity_type: {type: string, nullable: false, description: Aerolínea o ruta-año.}
      entity_key: {type: string, nullable: false, description: Entidad afectada.}
      period_id: {type: string, nullable: false, description: Periodo observado.}
      metric_key: {type: string, nullable: false, description: Métrica observada.}
      observed_value: {type: float, nullable: false, description: Valor observado.}
      expected_value: {type: float, nullable: false, description: Referencia del detector.}
      anomaly_score: {type: float, nullable: false, description: Intensidad estandarizada o relativa.}
      direction: {type: string, nullable: false, allowed_values: [above, below], description: Por encima o por debajo.}
      severity: {type: string, nullable: false, allowed_values: [low, medium, high], description: Severidad analítica.}
      event_matched: {type: bool, nullable: false, description: Coincidencia temporal con evento conocido.}
      event_title: {type: string, nullable: true, description: Evento cercano si existe.}
      event_date: {type: datetime, nullable: true, description: Fecha del evento cercano.}
      explanation: {type: string, nullable: false, description: Lectura accionable.}
      source_tables: {type: string, nullable: false, description: Linaje de tablas.}
      model_run_id: {type: string, nullable: false, description: Corrida determinista.}
  dim_cluster_assignments:
    stage: 7
    grain: [model_run_id, exercise, entity_key, period_id]
    primary_key: [record_id]
    columns:
      record_id: {type: string, nullable: false, unique: true, regex: '^rec_[0-9a-f]{64}$', description: Identificador estable del resultado de clustering.}
      model_run_id: {type: string, nullable: false, description: Corrida determinista.}
      exercise: {type: string, nullable: false, description: Rutas o trimestres.}
      entity_type: {type: string, nullable: false, description: Grano del ejercicio.}
      entity_key: {type: string, nullable: false, description: Ruta o aerolínea.}
      period_id: {type: string, nullable: false, description: Año o trimestre.}
      cluster_id: {type: int, nullable: false, description: Etiqueta técnica.}
      cluster_name: {type: string, nullable: false, description: Nombre de negocio.}
      k: {type: int, nullable: false, description: Número elegido de clusters.}
      silhouette: {type: float, nullable: false, description: Silueta global.}
      stability_ari: {type: float, nullable: false, description: ARI promedio entre semillas.}
      pca_1: {type: float, nullable: false, description: Primera componente para visualización.}
      pca_2: {type: float, nullable: false, description: Segunda componente para visualización.}
      features_json: {type: string, nullable: false, description: Features sin perder escala original.}
      name_validation_status: {type: string, nullable: false, description: Estado de revisión del nombre.}
  fact_study_results:
    stage: 7
    grain: [model_run_id, study_key]
    primary_key: [record_id]
    columns:
      record_id: {type: string, nullable: false, unique: true, regex: '^rec_[0-9a-f]{64}$', description: Identificador estable calculado con la tabla y su grano natural.}
      study_key: {type: string, nullable: false, description: Estudio de alto valor.}
      title_es: {type: string, nullable: false, description: Título de negocio.}
      finding_es: {type: string, nullable: false, description: Hallazgo escrito en español.}
      estimate: {type: float, nullable: true, description: Estimación principal cuando existe.}
      unit: {type: string, nullable: false, description: Unidad de la estimación.}
      period_id: {type: string, nullable: false, description: Ventana o corte analítico.}
      comparison: {type: string, nullable: false, description: Base de comparación.}
      confidence: {type: string, nullable: false, allowed_values: [alta, media, baja], description: Confianza cualitativa.}
      caveat: {type: string, nullable: false, description: Limitación material.}
      source_tables: {type: string, nullable: false, description: Linaje de tablas.}
      model_run_id: {type: string, nullable: false, description: Corrida determinista.}
  fact_route_traffic_summary:
    stage: 8
    grain: [carrier_key, market_key, period_id]
    primary_key: [record_id]
    foreign_keys:
      - columns: [carrier_key]
        references: {table: dim_carrier, columns: [carrier_key]}
    columns:
      record_id: {type: string, nullable: false, unique: true, regex: '^rec_[0-9a-f]{64}$', description: Identificador estable calculado con la tabla y su grano natural.}
      carrier_key: {type: string, nullable: false, description: Aerolínea.}
      market_key: {type: string, nullable: false, description: Mercado bidireccional.}
      period_id: {type: string, nullable: false, description: Mes calendario.}
      origin_iata: {type: string, nullable: false, description: Primer extremo canónico.}
      dest_iata: {type: string, nullable: false, description: Segundo extremo canónico.}
      seats: {type: float, nullable: false, description: Asientos mensuales agregados.}
      passengers: {type: float, nullable: false, description: Pasajeros mensuales agregados.}
      asm_miles: {type: float, nullable: false, description: ASM mensuales agregados.}
      rpm_miles: {type: float, nullable: false, description: RPM mensuales agregados.}
      departures: {type: float, nullable: false, description: Salidas realizadas agregadas.}
      load_factor: {type: float, nullable: true, description: RPM dividido entre ASM.}
      source_files: {type: string, nullable: false, description: Archivos contribuyentes.}
      source_hash: {type: string, nullable: false, description: Hash determinista de linaje.}
      ingested_at: {type: datetime, nullable: false, description: Última ingesta contribuyente.}
  fact_spread_decomposition:
    stage: 8
    grain: [period_id, component_key]
    primary_key: [record_id]
    columns:
      record_id: {type: string, nullable: false, unique: true, regex: '^rec_[0-9a-f]{64}$', description: Identificador estable calculado con la tabla y su grano natural.}
      period_id: {type: string, nullable: false, description: Trimestre final de la descomposición.}
      comparison_period_id: {type: string, nullable: false, description: Trimestre inicial.}
      component_key: {type: string, nullable: false, description: Precio, combustible, residual estructural o FX.}
      component_name_es: {type: string, nullable: false, description: Etiqueta para la visualización.}
      contribution: {type: float, nullable: true, description: Aporte al cambio en centavos por ASK-km.}
      display_order: {type: int, nullable: false, description: Orden del waterfall.}
      is_identified: {type: bool, nullable: false, description: Distingue estimación de componente no identificable.}
      caveat: {type: string, nullable: false, description: Limitación de interpretación.}
      source_tables: {type: string, nullable: false, description: Linaje de tablas.}
  fact_dashboard_coverage:
    stage: 8
    grain: [carrier_key, metric_key, period_type, segment]
    primary_key: [record_id]
    foreign_keys:
      - columns: [carrier_key]
        references: {table: dim_carrier, columns: [carrier_key]}
      - columns: [metric_key]
        references: {table: dim_metric, columns: [metric_key]}
    columns:
      record_id: {type: string, nullable: false, unique: true, regex: '^rec_[0-9a-f]{64}$', description: Identificador estable calculado con la tabla y su grano natural.}
      carrier_key: {type: string, nullable: false, description: Aerolínea.}
      metric_key: {type: string, nullable: false, description: Métrica.}
      period_type: {type: string, nullable: false, allowed_values: [month, quarter, year], description: Mes, trimestre o año.}
      segment: {type: string, nullable: false, allowed_values: [total, domestic, international, transborder], description: Segmento reportado.}
      observations: {type: int, nullable: false, description: Periodos con fila.}
      first_period: {type: string, nullable: false, description: Primer periodo.}
      last_period: {type: string, nullable: false, description: Último periodo.}
      expected_periods: {type: int, nullable: false, description: Periodos calendario entre extremos.}
      coverage_pct: {type: float, nullable: false, description: Observaciones sobre periodos esperados.}
      null_values: {type: int, nullable: false, description: Filas con valor nulo.}
  dim_source_artifact:
    stage: 9
    grain: [source_file, artifact_sha256]
    primary_key: [artifact_id]
    foreign_keys:
      - columns: [source_key]
        references: {table: dim_source, columns: [source_key]}
    columns:
      artifact_id: {type: string, nullable: false, unique: true, regex: '^art_[0-9a-f]{64}$', description: Identificador estable del archivo Bronze físico.}
      source_key: {type: string, nullable: false, description: Fuente canónica propietaria del artefacto.}
      source_system: {type: string, nullable: false, description: Identificador técnico registrado en el manifiesto.}
      source_file: {type: string, nullable: false, description: Ruta POSIX relativa dentro de Bronze.}
      source_url: {type: string, nullable: false, description: URL HTTPS pública registrada en la descarga.}
      is_direct_public_artifact: {type: bool, nullable: false, description: Indica si la política permite enlazar directamente el archivo.}
      downloaded_at: {type: datetime, nullable: false, description: Fecha de descarga del artefacto.}
      artifact_sha256: {type: string, nullable: false, regex: '^[0-9a-f]{64}$', description: SHA-256 de los bytes originales e inmutables.}
      artifact_format: {type: string, nullable: false, description: Extensión o formato físico normalizado.}
      content_type: {type: string, nullable: false, description: Tipo MIME declarado sin parámetros.}
      byte_size: {type: int, nullable: false, min: 0, description: Tamaño físico del archivo.}
      http_status: {type: int, nullable: false, min: 0, description: Estado HTTP conservado por la descarga.}
      download_method: {type: string, nullable: false, description: Método utilizado para obtener el artefacto.}
      logical_key: {type: string, nullable: false, description: Identidad lógica versionada de la descarga.}
      logical_version: {type: int, nullable: false, min: 1, description: Número monotónico de la versión lógica.}
      filename_version: {type: int, nullable: false, min: 1, description: Versión incluida en el nombre inmutable.}
      is_latest_version: {type: bool, nullable: false, description: Indica la última versión conocida de la clave lógica.}
  bridge_record_lineage:
    stage: 9
    grain: [lineage_link_id]
    primary_key: [lineage_link_id]
    foreign_keys:
      - columns: [artifact_id]
        references: {table: dim_source_artifact, columns: [artifact_id]}
    columns:
      lineage_link_id: {type: string, nullable: false, unique: true, regex: '^lnk_[0-9a-f]{64}$', description: Identificador estable de la relación de linaje.}
      record_id: {type: string, nullable: false, regex: '^rec_[0-9a-f]{64}$', description: Registro Gold o analítico documentado.}
      table_name: {type: string, nullable: false, regex: '^[a-z][a-z0-9_]*$', description: Tabla propietaria del record_id.}
      lineage_type: {type: string, nullable: false, allowed_values: [direct_artifact, derived, curated], description: Tipo explícito de linaje.}
      link_type: {type: string, nullable: false, allowed_values: [artifact, parent_record, declaration], description: Clase de contribuyente representado por la fila.}
      lineage_status: {type: string, nullable: false, allowed_values: [resolved_to_artifact, resolved_to_parent_record, declared_without_artifact], description: Estado de resolución del enlace.}
      has_direct_artifact: {type: bool, nullable: false, description: Indica si el registro tiene al menos un archivo directo.}
      artifact_id: {type: string, nullable: true, regex: '^art_[0-9a-f]{64}$', description: Artefacto contribuyente cuando el enlace es directo.}
      artifact_sha256: {type: string, nullable: true, regex: '^[0-9a-f]{64}$', description: SHA-256 del archivo original; nunca un hash derivado.}
      parent_record_id: {type: string, nullable: true, regex: '^rec_[0-9a-f]{64}$', description: Registro padre cuando el resultado es derivado.}
      lineage_fingerprint: {type: string, nullable: false, regex: '^[0-9a-f]{64}$', description: Fingerprint de la combinación completa de contribuyentes.}
      lineage_note: {type: string, nullable: true, description: Declaración explícita cuando no existe un artefacto directo.}
